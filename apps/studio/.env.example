# API Configuration
PUBLIC_API_BASE_URL=http://localhost:8787/api

# Auth Cookie Domain (production only)
VITE_AUTH_DOMAIN=.pub.wiki

# Sandbox Site URL (use {id} placeholder for per-instance subdomain)
PUBLIC_SANDBOX_SITE_URL=http://{id}.sandbox.localhost:4001
